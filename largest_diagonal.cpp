#include <bits/stdc++.h>
using namespace std;

const int MAX = 100;
int main()
{
    int n, m;
    cin >> n >> m;
    int mat[n][m];
    for (int i = 0; i < n; i++)
    {
        for (int j = 0; j < m; j++)
        {
            cin >> mat[i][j];
        }
    }
    largest[50][50];
    for()

    return 0;
}